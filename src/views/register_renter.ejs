<html>
    <head>
        <title>ReYoCa:Create an account</title>
    </head>
    <body>
        <form action="http://localhost:3000/login/create-renter" method="POST">
            <label for="firstname">First Name</label>
            <input type="text" name="firstname" required>
            <label for="lastname">Last Name</label>
            <input type="text" name="lastname"><br><br>
            <label for="email">Email</label>
            <input type="email" name="email" required><br><br>
            <label for="password">Password</label>
            <input type="password" name="password" id="password" required>
            <label for="c_password">Confirm Password</label>
            <input type="password" name="c_password" id="confirm_password" required><br><br>
            <label for="phone">Contact Number</label>
            <input type="number" name="phone" id="phone"><br><br>
            <label for="gov_id">Gender</label>
            <select name="gender" id="gender" required>
                <option value="male">Male</option>
                <option value="female">Female</option>
              </select><br><br>
            <input type="submit" value="Register">
        </form>
        <script>
            var password = document.getElementById("password"), confirm_password = document.getElementById("confirm_password");

            function validatePassword(){
                if(password.value != confirm_password.value) {
                    confirm_password.setCustomValidity("Passwords Don't Match");
                } else {
                    confirm_password.setCustomValidity('');
                }
            }
            password.onchange = validatePassword;
            confirm_password.onkeyup = validatePassword;
            
            // var DL = document.getElementById("dl")
            // function validateDL(){
            //     if(!DL.value.startsWith("DL")) {
            //         DL.setCustomValidity("Driving License not Valid");
            //     } else {
            //         DL.setCustomValidity('');
            //     }
            // }
            // DL.onchange = validateDL;
            // DL.onkeyup = validateDL;

            var phone = document.getElementById("phone")
            function validatephone(){
                if(String(phone.value).length!=10) {
                    phone.setCustomValidity("Number not Valid");
                } else {
                    phone.setCustomValidity('');
                }
            }
            phone.onchange = validatephone;
            phone.onkeyup = validatephone;
        </script>
    </body>

</html>